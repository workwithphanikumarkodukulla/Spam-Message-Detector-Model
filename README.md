# Spam-Message-Detector-Model
Here it is:  SpamGuard is a Flask web app that detects email &amp; SMS spam in real time using a Linear SVM model trained with NLTK preprocessing. Achieves 98.03% accuracy and 97.74% precision. Features a dark-mode UI built with Bootstrap 5 and Material Icons, delivering instant AJAX predictions — no page reload required.
